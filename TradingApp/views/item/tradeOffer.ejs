<%- include('../partials/header.ejs')%>
<table class="table">
    <thead>
      <tr>
        <th scope="col">Item Name</th>
        <th scope="col">Status</th>
        <th scope="col">Action</th>
      </tr>
    </thead>
    <tbody>
        <%if(categoryItems.length > 0) {%>
            <%categoryItems.forEach(category => {%>
                    <tr>
                        <td><a href="/trades/<%= category.items._id%>?category=<%= category._id%>"><%= category.items.itemName%></a></td>
                        <td><%= category.items.status%></td>
                        <td><%= category.items.status%></td>
                        <form>
                            <input type="hidden" id="itemAgainstId" name="itemAgainstId" value="<%= itemAgainst.items[0]._id%>">
                            <input type="hidden" id="itemAgainstUser" name="itemAgainstUser" value="<%= itemAgainst.items[0].user%>">
                            <td><button type="submit" formmethod="POST" formaction="/trades/<%= category.items._id%>/itemOffer" class=" btn btn-dark dark-weight-btn" >Choose</button></td>
                        </form>
                        
                    </tr>
            <%})%>
        <%}%>

    </tbody>
  </table>
<%- include('../partials/footer.ejs')%>